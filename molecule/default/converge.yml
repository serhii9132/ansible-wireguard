---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: "Include ansible-role-wireguard"
      ansible.builtin.include_role:
        name: "../../../ansible-role-wireguard/" # noqa: role-name
